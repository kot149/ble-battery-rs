# ble-battery-rs

A not-so-sophisticated sample implementation to get battery levels and RSSI of [ZMK](https://zmk.dev) BLE devices using [Bluest](https://github.com/alexmoon/bluest).

Battery level retrieval works both on macOS and Windows.
RSSI is only supported on macOS.

## Usage

```bash
cargo run
```
